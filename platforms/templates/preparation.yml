variables:
  force32BitNode: eq(variables.BUILD_ARCH, 'x86')

steps:

  # Common
  - task: NodeTool@0
    inputs:
      versionSpec: 12.16.3
      force32bit: ${{ variables.force32BitNode }}
    displayName: Install Node.js v12.16.3
